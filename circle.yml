# Download blog content.
dependencies:
  pre:
    - mkdir posts
    - git clone ${JACQUI} posts/jacqui
    - git clone ${HAWKEYE} posts/hawkeye

# Run BitBalloon deployment script.
deployment:
  production:
    branch: master
    commands:
      - npm run deploy
